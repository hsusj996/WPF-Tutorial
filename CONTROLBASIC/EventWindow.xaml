<Window x:Class="CONTROLBASIC.EventWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CONTROLBASIC"
        mc:Ignorable="d"
        Title="EventWindow" Height="450" Width="800">

    <Window.Resources>
        <BeginStoryboard x:Key="mystoryboard">
            <Storyboard>
                <DoubleAnimation Storyboard.TargetName="button1"
                                                 Storyboard.TargetProperty="Width"
                                                 From="100" To="200" Duration="0:0:5"/>
            </Storyboard>
        </BeginStoryboard>
    </Window.Resources>
    
    <StackPanel>
        <Button Click="Button_Click_1" x:Name="button1" Content="ClickMe1" FontSize="20"></Button>
        <Button Click="Clicked" Content="ClickMe2" FontSize="20">
            <x:Code>
                <![CDATA[
                    void Clicked(object sender, RoutedEventArgs e)
                    {
                        button1.Content = "Hello World!";
                    }
                ]]>
            </x:Code>
        </Button>

        <Button Content="ClickMe3" FontSize="20">
            <Button.Triggers>
                <EventTrigger RoutedEvent="Button.Click">
                    <EventTrigger.Actions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName ="button1"
                                                 Storyboard.TargetProperty="Width"
                                                 From="100" To="200" Duration="0:0:5"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger.Actions>
                </EventTrigger>
            </Button.Triggers>
        </Button>

        <Button Content="ClickMe4" FontSize="20">
            <Button.Triggers>
                <EventTrigger RoutedEvent="Button.Click">
                    <StaticResource ResourceKey="mystoryboard"></StaticResource>
                </EventTrigger>
            </Button.Triggers>
        </Button>
    </StackPanel>
</Window>
