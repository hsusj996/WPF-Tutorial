<Window x:Class="STYLE2.Example3"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:STYLE2"
        mc:Ignorable="d"
        Title="Example3" Height="450" Width="800">

    <Window.Resources>
        <Style x:Key="MouseOverHightlightStyle1">
            <Setter Property="Control.FontSize" Value="30" />
            <EventSetter Event="FrameworkElement.MouseEnter" Handler="element_MouseEnter" />
            <EventSetter Event="FrameworkElement.MouseLeave" Handler="element_MouseLeave" />
        </Style>

        <Style x:Key="MouseOverHightlightStyle2">
            <Setter Property="Control.FontSize" Value="30" />
            <Style.Triggers>
                <Trigger Property="Control.IsMouseOver" Value="True">
                    <Setter Property="Control.Background" Value="Yellow" />
                </Trigger>
                <Trigger Property="Control.IsMouseOver" Value="False">
                    <Setter Property="Control.Background" Value="{x:Null}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        
    </Window.Resources>

    <StackPanel>
        <Label Content="Label1" Style="{StaticResource MouseOverHightlightStyle1}"/>
        <Label Content="Label1" Style="{StaticResource MouseOverHightlightStyle2}"/>
    </StackPanel>
    
</Window>
